export enum ENUM_TAG_TYPE {
  user = "user",
  admin = "admin",
  faculty = "faculty",
  department = "department",
}

export const tagTypes = [
  ENUM_TAG_TYPE.user,
  ENUM_TAG_TYPE.admin,
  ENUM_TAG_TYPE.faculty,
  ENUM_TAG_TYPE.department,
];
